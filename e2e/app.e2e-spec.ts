import {AppPage} from './app.po';

describe('angular-io-v42 App', function() {
  let page: AppPage;

  beforeEach(() => {
    page = new AppPage();
  });

  it('should do something', () => {
    expect(1).toBe(1);
  });
});
